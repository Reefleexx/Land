import React from 'react'
import classes from './Menu.module.scss'

const Menu = (props) => {
    return(
        <div className={classes.Menu}>
            HI
        </div>
    )
}

export default Menu